// MongoDB does not require column migrations.
// Roles and schema are managed automatically by Mongoose models.
// Run the main migrate script instead: npx tsx src/migrate.ts
console.log('No SQL role migration needed for MongoDB. Run: npx tsx src/migrate.ts');
